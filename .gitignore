# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual env
.venv/
venv/

# Env / secrets
.env
**/.env

# Test / tooling
.pytest_cache/
.coverage
htmlcov/

# Manim / render output
media/
backend/scripts/_benchmark_output/
eval/fixtures/*.pptx

# meta-template local SQLite store
backend/var/

# SDD scratch (not committed)
.superpowers/
.worktrees/
docs/superpowers/

# Local source/reference materials (not committed)
/eval set/
/project description.md
/project_pitch_slides.pptx
/demo.ps1

# OS
.DS_Store

# Node / frontend build artifacts
node_modules/
.vite/

# Playwright E2E artifacts
frontend/playwright-report/
frontend/test-results/
frontend/blob-report/
frontend/playwright/.cache/

# Impeccable design-hook state (generated)
.impeccable/

# Claude Code local config. Named files, not the whole directory:
# .claude/settings.json is also the conventional home for shared project config
# (permissions, hooks, project skills), so keep that door open.
.claude/settings.json
.claude/settings.local.json

# Skill files (local tooling, not project code)
.claude/skills/
.claude/scripts/
.claude/commands/
.claude/worktrees/
.github/skills/
.github/agents/impeccable-*.agent.md
.github/hooks/impeccable.json
